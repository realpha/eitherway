{
  "tasks": {
    "build:npm": "deno run -A ./scripts/build_npm.ts",
    "test:full": "deno test -A --doc --coverage=./cov",
    "test:cov": "deno test -A --coverage=./cov",
    "test:ci": "deno test -A --doc --coverage=./cov ./lib"
  }
}
